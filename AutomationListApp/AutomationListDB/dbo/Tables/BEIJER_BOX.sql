CREATE TABLE [dbo].[BEIJER_BOX] (
    [Id]        INT           PRIMARY KEY IDENTITY (1, 1) NOT NULL,
    [Name]      VARCHAR (200) NULL,
    [IpAddress] VARCHAR (100) NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

